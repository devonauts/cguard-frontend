import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const guardShiftImporterSelectors = importerSelectors(
  'guardShift.importer',
);

export default guardShiftImporterSelectors;
