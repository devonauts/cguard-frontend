import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const inventoryImporterSelectors = importerSelectors(
  'inventory.importer',
);

export default inventoryImporterSelectors;
