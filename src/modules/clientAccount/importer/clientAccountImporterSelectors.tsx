import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const clientAccountImporterSelectors = importerSelectors(
  'clientAccount.importer',
);

export default clientAccountImporterSelectors;
