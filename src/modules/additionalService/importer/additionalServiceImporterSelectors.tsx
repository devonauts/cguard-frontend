import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const additionalServiceImporterSelectors = importerSelectors(
  'additionalService.importer',
);

export default additionalServiceImporterSelectors;
