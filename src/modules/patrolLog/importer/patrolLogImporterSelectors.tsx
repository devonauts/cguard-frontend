import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const patrolLogImporterSelectors = importerSelectors(
  'patrolLog.importer',
);

export default patrolLogImporterSelectors;
