import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const patrolCheckpointImporterSelectors = importerSelectors(
  'patrolCheckpoint.importer',
);

export default patrolCheckpointImporterSelectors;
